import { Box } from '@mui/material';
import NavbarPage from 'scenes/navbar';

const HomePage = () => {
    return (
        <Box>
            <NavbarPage />
        </Box>
    );
};

export default HomePage;
